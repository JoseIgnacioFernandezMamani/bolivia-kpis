# NOTE: scrapy 2.11.2 has a known DoS vulnerability (GHSA-xxxx) with no patched
# version available as of 2026-02-19 (affects >= 0.7, <= 2.14.1).
# Monitor https://github.com/advisories for a patch and upgrade when released.
scrapy==2.11.2
playwright==1.44.0
scrapy-playwright==0.0.34
pdfplumber==0.11.1
httpx==0.27.0
python-dotenv==1.0.1
redis==5.0.4
psycopg2-binary==2.9.9
